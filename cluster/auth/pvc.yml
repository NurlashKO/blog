apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-data
  namespace: auth
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
